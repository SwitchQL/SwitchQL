language: node_js

node_js:
  - "node"

jobs:
  include:
    - stage: test
      os: osx
      script: npm run test:ci

    - stage: build
      os: linux
      script: npm run dist

    - stage: build
      os: linux
      script: npm run dist

    - stage: build
      os: linux
      script: npm run dist
